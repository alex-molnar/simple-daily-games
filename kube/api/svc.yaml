apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: simple-daily-games
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: api
  type: ClusterIP