apiVersion: v1
kind: Service
metadata:
  name: landing-page
  namespace: simple-daily-games
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: landing-page
  type: ClusterIP